<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">

    </style>

    <!-- Toolbar  -->
    <style name="AppTheme.AppBar" parent="@style/ThemeOverlay.Material3.Light" />

    <style name="AppTheme.Toolbar" parent="@style/ThemeOverlay.Material3" />

</resources>